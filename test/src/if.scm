(begin
    (if #t
        (begin (write "True1")(newline))
        (begin (write "False1")(newline))
    )
    (if #f
        (begin (write "True2")(newline))
        (begin (write "False2")(newline))
    )
)
