{
  "name": "webschembly-js",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "benchmark": "npm run build && node --expose-gc dist/e2e.benchmark.js",
    "benchmark-turbofan-only": "npm run build && node --expose-gc --no-liftoff dist/e2e.benchmark.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tinybench": "^5.0.1",
    "tsx": "^4.20.3",
    "typescript": "^5.5.0",
    "vitest": "^3.0.6"
  }
}
